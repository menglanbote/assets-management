export const token = state => state.token

export const username = state => state.username

export const isCollapse = state => state.isCollapse

export const historyPath = state => state.historyPath

export const currentNavPath = state => state.currentNavPath

export const ministryList = state => state.ministryList

export const menuList = state => state.menuList

export const tipList = state => state.tipList

export const authorInfo = state => state.authorInfo

export const isNavShow = state => state.isNavShow;

export const timer5min = state => state.timer5min;

export const lastTime = state => state.lastTime;

export const endTime30s = state => state.endTime30s;

export const endTime5s = state => state.endTime5s;